package functionDemo

object switch {
  def main(args: Array[String]): Unit = {
    var sign = 0
    var ch1 = '-'
    ch1 match {
      case '+' => sign = 1
      case '-' => sign = -1
      case _=> sign = 0
    }
    println(sign)
  }
}